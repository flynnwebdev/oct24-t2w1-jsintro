// Python
// def add(x, y):
//     return x + y
//
// lambda (x, y): x + y

const add = (x, y) => x + y
const square = x => x ** 2

console.log(add(10, 34))
console.log(square(5))

numbers = [1, 2, 3, 4, 5]
const result = numbers.map(x => x ** 3)
console.log(result)
